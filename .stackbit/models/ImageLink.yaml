name: ImageLink
label: Image link
groups:
  - BlockComponents
fields:
  - type: string
    name: link
    default: 
  - type: image
    name: image
    required: true
    default: 
  - type: string
    name: altText
    label: Alt text (for the visually impaired)
    default: 