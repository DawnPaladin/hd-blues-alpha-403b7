name: ImageLink
label: Image link
groups:
  - BlockComponents
fields:
  - type: string
    name: link
    default: 
  - type: string
    name: imgUrl
    label: Image URL
    required: true
    default: 
  - type: string
    name: altText
    label: Alt text describing the image (for the visually impaired)