name: Image
label: Image
groups:
  - BlockComponents
fields:
  - type: string
    name: link
    default:
  - type: image
    name: image
    required: true
  - type: string
    name: altText
    label: Alt text (for the visually impaired)
    default:
labelField: altText
